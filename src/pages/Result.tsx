import { Waiting } from './Waiting';

export function Result() {
  return <Waiting message="Svar kommer på storskjerm. Hadde ikke nok tid til å utvikle denne siden. Cutting corners.. Tale as old as time.." />
}
